# Requires Python 3.11 or 3.12 (the ML stack below has no wheels for 3.13/3.14 yet).
# Create your venv accordingly:  python3.12 -m venv .venv

# --- Orchestration ---
langgraph==0.2.45
langgraph-checkpoint-postgres==2.0.1
redis==5.0.8
psycopg[binary]>=3.2,<3.4

# --- LLM + embeddings (unified, OpenAI-compatible, with fallbacks) ---
litellm==1.51.0
boto3==1.35.50            # Bedrock, Athena, DynamoDB, S3
sqlglot==25.24.0          # SQL validation / SELECT-only guard

# --- Retrieval ---
opensearch-py==2.7.1
requests-aws4auth==1.3.1

# --- Structured store (local Athena stand-in) ---
duckdb==1.1.3

# --- API ---
fastapi==0.115.4
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.6.1
python-dotenv>=1.0,<2.0

# --- Ingestion ---
pypdf==5.1.0

# --- Observability + evaluation ---
langfuse>=3.0.0,<4.0.0
nltk>=3.9
python-multipart>=0.0.9
openai>=1.0
python-jose[cryptography]==3.3.0
